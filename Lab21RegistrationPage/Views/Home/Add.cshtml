@using Lab21RegistrationPage.Models;
@{
    ViewBag.Title = "Add";
}
<h2>Book Added</h2>
Cart:
<ul>
    @foreach (Item x in (List<Item>)(Session["Cart"]))
    {
        <li>@x.Name by @x.Price</li>
    }
</ul>

